const names = ['Edoardo', 'Simone', 'Francesco'];

// Stampa in console tutti i nomi
// Risultato: 'Edoardo', 'Simone', 'Francesco'

// forEach method
const printName = names.forEach(name => console.log(name))


// loop for traditional
for (let i = 0; i < names.length; i++){
    logName = names[i];
    console.log(logName)
}